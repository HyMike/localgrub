{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run --prefix order-service start:producer\" \"npm run --prefix restaurant-service start:consumer\"",
    "build": "tsc -p tsconfig.build.json",
    "test": "vitest",
    "test:run": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:ui": "vitest --ui",
    "test:order": "npm run --prefix order-service test",
    "test:restaurant": "npm run --prefix restaurant-service test",
    "test:payment": "npm run --prefix payment-service test",
    "test:notification": "npm run --prefix notification-service test"
  },
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^22.15.18",
    "@types/supertest": "^6.0.3",
    "@vitest/coverage-v8": "^3.2.4",
    "@vitest/ui": "^3.2.4",
    "concurrently": "^9.1.2",
    "supertest": "^7.1.4",
    "ts-node": "^10.9.2",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  },
  "dependencies": {
    "amqplib": "^0.10.8",
    "dotenv": "^16.5.0",
    "firebase-admin": "^13.4.0",
    "install": "^0.13.0",
    "npm": "^11.4.2"
  }
}
